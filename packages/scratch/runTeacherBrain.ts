/*const parsedDocument = await parseDocument(pdf);

const extracted =
  await conceptExtractor.extract(parsedDocument);

const normalized =
  normalizeConcepts(extracted);

const graph =
  buildKnowledgeGraph(normalized);

await repository.save(graph);
console.log(graph);
*/